# Copie para .env e ajuste os valores (nunca commitar o .env)

DB_USER=postgres
DB_PASSWORD=change_me

TEST_DB_USER=test
TEST_DB_PASSWORD=change_me

# PgAdmin — docker compose --profile tools up
PGADMIN_EMAIL=admin@loja.com
PGADMIN_PASSWORD=change_me
